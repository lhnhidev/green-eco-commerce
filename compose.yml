version: '3.8'

services:
  redis:
    image: redis:7.2-alpine
    container_name: green_eco_commerce_redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --requirepass 4C@aymantruocnha --appendonly yes

volumes:
  redis_data:
